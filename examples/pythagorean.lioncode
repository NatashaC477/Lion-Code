~code to find the length of the hypotenuse in a right triangle~
ignite findHypotenuse(a,b) |
    c_squared = (a * a) + (b * b)
    c = sqrt(c_squared)
    serve c
|

result = findHypotenuse(3,4)
roar -${result}-
